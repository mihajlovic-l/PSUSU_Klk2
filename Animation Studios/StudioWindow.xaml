<Window x:Class="Animation_Studios.StudioWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Studio" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Name" />
        <TextBox x:Name="NameBox" Grid.Row="0" Margin="80,0,0,0" Width="250" />

        <TextBlock Text="Country" Grid.Row="1" />
        <TextBox x:Name="CountryBox" Grid.Row="1" Margin="80,0,0,0" Width="250" />

        <TextBlock Text="Headquarters" Grid.Row="2" />
        <TextBox x:Name="HQBox" Grid.Row="2" Margin="80,0,0,0" Width="250" />

        <TextBlock Text="Founded" Grid.Row="3" />
        <DatePicker x:Name="FoundedPicker" Grid.Row="3" Margin="80,0,0,0" Width="150" />

        <TextBlock Text="Number of employees" Grid.Row="4" />
        <TextBox x:Name="EmployeesBox" Grid.Row="4" Margin="150,0,0,0" Width="80" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="5" Margin="0,10,0,0">
            <Button x:Name="OkButton" Width="75" Margin="0,0,10,0">OK</Button>
            <Button Width="75" Click="Cancel_Click">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
